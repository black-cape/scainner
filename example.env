# ScAInner Configuration
# Copy this file to .env and adjust the values as needed

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# The OpenMHz API endpoint to poll for calls
# Example: https://api.openmhz.com/arlco/calls
# Find endpoints at: https://openmhz.com/systems
ENDPOINT=

# ============================================================================
# SLACK NOTIFICATIONS (Optional)
# ============================================================================

# Slack webhook URL for sending notifications when transcriptions match patterns
# Leave empty to disable Slack notifications
# To create a webhook: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# Python regex patterns that specify which transcriptions should trigger notifications
# Multiple patterns should be separated by &&& (triple ampersand)
# If not set and SLACK_WEBHOOK_URL is set, all transcriptions will trigger notifications
# Example: '(?i)\bblack\s+cape\b&&&(?i)\ballston\b'
NOTIFICATION_PATTERNS=

# ============================================================================
# WHISPER MODEL CONFIGURATION (Optional)
# ============================================================================

# The Whisper model size to use for transcription
# Options: distil-small.en, distil-medium.en, distil-large-v3, base, small, medium, large, large-v2, large-v3
# Smaller models are faster but less accurate. Larger models are more accurate but slower.
# Default: distil-small.en
WHISPER_MODEL_SIZE=distil-small.en

# The computation type for fast-whisper
# Options: default, int8, int8_float16, int16, float16, float32
# int8 is fastest but may reduce accuracy. default uses the recommended type for your hardware.
# Default: default
WHISPER_COMPUTE_TYPE=default

# ============================================================================
# POLLING CONFIGURATION (Optional)
# ============================================================================

# How many seconds to wait between each poll for new calls
# Default: 900
FETCH_INTERVAL=900

# ============================================================================
# MONGODB CONFIGURATION (Optional)
# ============================================================================

# MongoDB hostname for storing transcriptions
# When using docker-compose, this should be: mongodb
# For local development, use: localhost
# If not set, ScAInner will not database calls
MONGO_DB_HOSTNAME=mongodb

# MongoDB port
# Default: 27017
MONGO_DB_PORT=27017

# MongoDB database name
# Default: scainner-db
MONGO_DB_NAME=scainner-db

